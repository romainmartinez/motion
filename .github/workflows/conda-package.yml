name: Conda package

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    # every sunday at 14:00 UTC
    # * is a special character in YAML so you have to quote this string
    - cron:  '0 14 * * SUN'

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    #          python-version: [3.5, 3.6, 3.7, 3.8]

    steps:
      - uses: actions/checkout@master

      - name: Install Miniconda
        run: |
          # Miniconda is already installed in github actions:
          source "$CONDA/etc/profile.d/conda.sh"
          hash -r
          conda config --set always_yes yes --set changeps1 no
          conda update -q conda
          conda info -a
#          conda create -q -n test-environment black
#          conda activate test-environment

      - name: Install Python with dependencies #${{ matrix.python-version }}
        run: |
          conda env create -f environment.yml
          conda activate motion

#      - name: Lint with Black
#        run: black --check .
#
#      - name: Test with Pytest
#        run: pytest --cov-report term-missing --color=yes --cov=motion tests
#
#      - name: Code coverage with Codecov
#        run: echo "todo"
